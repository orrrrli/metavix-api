name: Deploy API to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: GH_PAT
          script: |
            set -e
            cd /home/rokev-dynamics/metavix-api
            git pull https://x-access-token:${GH_PAT}@github.com/orrrrli/metavix-api.git main
            dotnet publish src/API/API.csproj -c Release -o /home/rokev-dynamics/metavix-api/publish
            sudo systemctl restart metavix-api
